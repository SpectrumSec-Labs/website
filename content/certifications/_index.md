---
title: "Certifications"
build:
  render: never
  list: always
---

Not a real page — this section exists only to feed the `{{< certifications >}}`
shortcode on `/about/`. Each certification is its own page bundle: a
`content/certifications/<slug>/index.md` with the badge image co-located in
the same folder. Nothing here is rendered or listed publicly.
